-- Spanish (Spain) localization for BetterBlizzFrames
-- Localización en español (España) para BetterBlizzFrames

local locale = GetLocale()
if locale ~= "esES" then return end

-- Use the global L table (L = BBF.L is set in locale.lua)
-- Falls back to English (enUS) if translation is not provided
-- Vuelve al inglés (enUS) si no se proporciona traducción

